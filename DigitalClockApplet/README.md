To run the applet make sure you are using `java 9` or lesser.

On linux/MacOS execute `make run` to run the applet.

On Windows do the following:

`javac DigitalClock.java`

`appletviewer DigitalClock.java`